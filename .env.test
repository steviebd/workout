# Test configuration template for Playwright E2E tests
# Note: TEST_USERNAME and TEST_PASSWORD are loaded from Infisical
# Run: bun run test:e2e

# Application URL
BASE_URL=http://localhost:8787

# WorkOS AuthKit selectors
PLAYWRIGHT_AUTH_EMAIL_SELECTOR=input[name="email"]
PLAYWRIGHT_AUTH_PASSWORD_SELECTOR=input[name="password"]
PLAYWRIGHT_AUTH_SUBMIT_SELECTOR=button[name="intent"]:not([data-method])
PLAYWRIGHT_AUTH_CONTINUE_SELECTOR=button:has-text("Continue")

# Debug mode (set to 1 for headed browser)
DEBUG=
